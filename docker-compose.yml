services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: bamadogym:latest
    container_name: bamado-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    env_file:
      - .env
    environment:
      CACHE_CONFIG: ${CACHE_CONFIG:-true}
    volumes:
      - storage_app:/var/www/html/storage/app
      - storage_logs:/var/www/html/storage/logs

volumes:
  storage_app:
  storage_logs:
